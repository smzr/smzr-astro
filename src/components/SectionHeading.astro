<div class="section-heading">
    <div class="content flow">
        <slot />
    </div>
</div>
<style lang="scss">
    .section-heading {
        position: relative;
        padding: 5rem 0;
    }
    .content {
        max-width: 640px;
        margin: 0 auto;
        text-align: center;

        :global(h2) {
            font-size: var(--s-huge);
        }

        :global(p){
            font-size: var(--s-lg);
        }
    }
</style>